<div class="container">
	<div class="row">
		<div class="col-10">
			<%= form_with model: [:publisher, @genre] do |f| %>
				<%= f.label :name %>
				<%= f.text_field :name, class:"form-control" %>
				<%= f.submit "作成", class:"btn btn-success" %>
			<% end %>
		</div>
	</div>
</div>